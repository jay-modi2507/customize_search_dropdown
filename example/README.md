# Example

This example demonstrates how to use the `customize_search_dropdown` package.

It includes:
- Single selection with local data.
- Multi-selection with validation.
- API simulation with pagination and search.
- Fully customized styling.

## Running the Example

1. Navigate to the example directory: `cd example`
2. Run the app: `flutter run`
